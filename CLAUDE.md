# Project conventions

## Versioning

- **Bump the `version` in `package.json` on every change that produces a new
  `.vsix`**, so the user can tell builds apart. Use semantic versioning
  (patch for fixes, minor for features). Mention the new version when handing
  over the build.
